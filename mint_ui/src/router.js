import loadMore from './components/loadMore.vue'

const router = {
        routes: [{
            path: "/",
            component: loadMore
        }]
    } //这里可以带有路由器的配置参数

export default router; //将路由器导出